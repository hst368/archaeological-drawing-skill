interface:
  display_name: "文物绘图"
  short_description: "证据约束的考古绘图工作流"
  default_prompt: "Use $archaeological-drawing to turn this artifact photo or description into an evidence-bounded archaeological drawing plan, prompt package, and risk-reviewed draft workflow."
policy:
  allow_implicit_invocation: true
